/* 月の画像をスライダーで表示 */
import React from 'react';

const Moon = (props) => {
    return (
        <div>
            {/* jsonから情報をもってきて、その日付に対応する画像を表示 */
            /* 画像はpublicフォルダに入れておく */
            }
        </div>
    );
    }